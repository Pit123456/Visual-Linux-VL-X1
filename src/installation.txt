=========================

// NodeJS Installation
sudo apt-get update
sudo apt install nodejs-legacy
sudo apt-get install npm

// Electron App Installation
sudo npm install -g electron-prebuilt

// Copy package.json to Destination App

=========================

// Start App Command

//Navigate to Electron App
go to main source directory i.e. Visual-Linux-VL-X1/src

//Start Electron App
npm start

=========================
